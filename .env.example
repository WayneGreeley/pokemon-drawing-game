# Lambda Function URL (from SAM deployment output)
VITE_LAMBDA_URL=https://your-lambda-url.lambda-url.region.on.aws/

# Note: Do NOT commit the actual .env file with real values
# Copy this file to .env and fill in your actual Lambda URL after deployment
